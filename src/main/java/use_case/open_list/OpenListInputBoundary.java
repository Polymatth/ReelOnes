package use_case.open_list;

/**
 * Input Boundary for actions which are related to the Open Lists use case.
 */
public interface OpenListInputBoundary {
    void execute(OpenListInputData inputData);
}
